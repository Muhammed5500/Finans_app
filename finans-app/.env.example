# =============================================================================
# FINANS APP - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local and update values as needed:
#   cp .env.example .env.local
# =============================================================================

# =============================================================================
# DATABASE (Required)
# =============================================================================
# Local development:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/finans_dev?schema=public

# =============================================================================
# OPENAI API (Required for AI features)
# =============================================================================
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Custom embedding model (default: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-small

# Optional: OpenAI model configuration for structured responses
# OPENAI_MODEL_FAST=gpt-4o-mini  # Fast model for quick responses
# OPENAI_MODEL_SMART=gpt-4o      # Smart model for complex tasks

# Optional: Enable OpenAI request logging
# LOG_OPENAI=true

# =============================================================================
# AUTHENTICATION (Optional - for API token auth)
# =============================================================================
# API_TOKEN=your-secret-api-token-here

# =============================================================================
# APPLICATION
# =============================================================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
